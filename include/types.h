#ifndef TYPES_H
#define TYPES_H

#include <stdint.h>
#include <stddef.h>

/* Standard OS types for bare-metal */
typedef int sig_atomic_t;
typedef unsigned int uint;

/* Add other basic types if needed */

#endif
